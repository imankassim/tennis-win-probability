# Secrets — never commit
.env
.env.*
!.env.example

# Raw third-party data (see docs/data_sheets/data_provenance.md — not redistributed)
data/raw/
*.parquet
*.duckdb

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/
node_modules/
# Regenerated by `next dev` on every start (generic Next.js version notes, not project content)
frontend/AGENTS.md
frontend/CLAUDE.md

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Model artefacts (tracked via the model registry, not git)
docs/model_cards/artefacts/

# OS / editor
.DS_Store
Thumbs.db
